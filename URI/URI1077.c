#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main ()
{
    int N, i, j;
    char expr[301], psfixa[301];

    scanf("%i", &N);

    for (i = 0; i < N; i++)
    {
        scanf("%s", expr);

       for (i = 0; i < strlen(expr); i++)
       {
           if (/* condition */)
           {
               /* code */
           }
           
       }
       
        
    }
    

    return 0;
}